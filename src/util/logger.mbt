///|
pub fn logger(msg : String) -> Unit {
  let debug = true
  if debug {
    println("logger: " + msg)
  }
}
pub fn debug(msg : String) -> Unit {
  let debug = true
  if debug {
    println("logger: " + msg)
  }
}
